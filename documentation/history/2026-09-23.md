# 2026-09-23

## Fixes

- "Mark all as read" failed with a 404: chats and threads were sent as `POST`; the web app uses `PATCH /internal_api/chat_rooms/mark_all_as_read?only_unread=true` and `PATCH /internal_api/chat_threads/mark_all_as_read` (notifications stay `POST`). All three steps are now tried and the error names the failed ones. Verified live: all three answer `200 {"success":true}`; the old `POST` on chat rooms answers 404.
- Row actions in the "what's new" list did nothing: a private `chooser()` method shadowed Obsidian's `chooser` field on `SuggestModal`, so every action threw. Renamed; a spec guards the names.
- Counts drop immediately when items are opened, read or archived (`ActivityWatcher.adjustCounts`); the next check confirms.
- Hand cursor on every clickable element of the plugin's UI.

## 1.1.1

- First background check ran a full interval after startup instead of 3 seconds: `onLayoutReady` re-armed the fresh timer. `start()` now reports whether it started; only a running watcher is re-armed.
- Opening the pane on a given page (command, notice, list) before its first load threw "The WebView must be attached to the DOM" and landed on the last page. The requested page is kept and loaded on the first `dom-ready`.

## 1.2.0

- System notifications: `desktopNotifications` = `always` (default) | `background` | `off`, replacing the 1.1 boolean `showDesktopNotifications` (on migrates to `always`, off to `off`). Previously they only showed while Obsidian was not focused.
- Whole-community watch only reads chat spaces the member belongs to (`is_space_member`); the settings list only those spaces. Refused lookups were already caught and never retried, but they cost requests.
- Verified: on Sébastien's desktop the web Notification API, Electron's native notification and `notify-send` all reach the notification server (Omarchy's quickshell).
- User guide: overview and tips brought up to date (notifications, inbox, read/archive, whole-community watch), toolbar section, and an "If you don't see system notifications" section (Windows, macOS, Linux).
